# [DO NOT EDIT] This file was generated by CUE (cue/).
$schema: https://json-schema.org/draft/2020-12/schema
$defs:
  InputItem:
    type: object
    description: Input dataset specification for metadata and sequences
    additionalProperties: false
    properties:
      metadata:
        type: string
        description: Path or URL to metadata TSV file (supports {a_or_b} template)
      name:
        type: string
        description: Unique identifier name for this input dataset
      sequences:
        type: string
        description: Path or URL to sequence FASTA file (supports {a_or_b} template)
    required:
      - name
  SubsampleConfigUnaligned:
    type: object
    additionalProperties: false
    properties:
      defaults:
        description: Default properties applied across all sample partitions
        $ref: '#/$defs/defaultProperties'
      samples:
        type: object
        description: Map of sample partition names (e.g. 'sample', 'recent', 'background') to their subsampling parameters
        additionalProperties:
          description: <sample_name>
          $ref: '#/$defs/filterSampleProperties'
    required:
      - samples
  defaultProperties:
    type: object
    additionalProperties: false
    properties:
      exclude:
        description: File(s) with list of strains to exclude.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      exclude_all:
        type: boolean
        description: |-
          Exclude all strains by default. Use this with the include arguments to
          select a specific subset of strains.
      exclude_ambiguous_dates_by:
        description: |-
          Exclude ambiguous dates by day (e.g., 2020-09-XX), month (e.g.,
          2020-XX-XX), year (e.g., 200X-10-01), or any date fields.
        enum:
          - any
          - day
          - month
          - year
      exclude_invalid:
        type: boolean
        description: Exclude sequences that contain invalid characters.
      exclude_where:
        description: |-
          Exclude sequences matching these conditions. Ex: "host=rat" or
          "host!=rat". Multiple values are processed as OR (matching any of those
          specified will be excluded), not AND.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      include:
        description: |-
          File(s) with list of strains to include regardless of priorities,
          subsampling, or absence of an entry in sequences.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      include_where:
        description: |-
          Include sequences with these values. ex: host=rat. Multiple values are
          processed as OR (having any of those specified will be included), not
          AND.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      max_date:
        description: Maximal cutoff for date (inclusive).
        anyOf:
          - type: integer
          - type: string
      max_length:
        type: integer
        description: Maximum length of the sequences, only counting valid characters.
      min_date:
        description: Minimal cutoff for date (inclusive).
        anyOf:
          - type: integer
          - type: string
      min_length:
        type: integer
        description: Minimal length of the sequences, only counting valid characters.
      non_nucleotide:
        type: boolean
        description: Deprecated, please use 'exclude_invalid' instead.
      query:
        type: string
        description: Filter sequences by attribute. Uses Pandas DataFrame query syntax.
      query_columns:
        description: Use alongside query to specify columns and data types in the format 'column:type'.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
  filterSampleProperties:
    type: object
    additionalProperties: false
    properties:
      context_sample:
        type: string
        description: Use the outputs from another sample as the inputs for this sample. Value must be a sample name.
      drop_sample:
        type: boolean
        description: Drop this sample from the final output
      exclude:
        description: File(s) with list of strains to exclude.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      exclude_all:
        type: boolean
        description: |-
          Exclude all strains by default. Use this with the include arguments to
          select a specific subset of strains.
      exclude_ambiguous_dates_by:
        description: |-
          Exclude ambiguous dates by day (e.g., 2020-09-XX), month (e.g.,
          2020-XX-XX), year (e.g., 200X-10-01), or any date fields.
        enum:
          - any
          - day
          - month
          - year
      exclude_invalid:
        type: boolean
        description: Exclude sequences that contain invalid characters.
      exclude_where:
        description: |-
          Exclude sequences matching these conditions. Ex: "host=rat" or
          "host!=rat". Multiple values are processed as OR (matching any of those
          specified will be excluded), not AND.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      group_by:
        description: Grouping columns for subsampling.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      group_by_weights:
        type: string
        description: TSV file defining weights for grouping.
      include:
        description: |-
          File(s) with list of strains to include regardless of priorities,
          subsampling, or absence of an entry in sequences.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      include_where:
        description: |-
          Include sequences with these values. ex: host=rat. Multiple values are
          processed as OR (having any of those specified will be included), not
          AND.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      max_date:
        description: Maximal cutoff for date (inclusive).
        anyOf:
          - type: integer
          - type: string
      max_length:
        type: integer
        description: Maximum length of the sequences, only counting valid characters.
      max_sequences:
        type: integer
        description: Select no more than this number of sequences (i.e. total sample size).
      min_date:
        description: Minimal cutoff for date (inclusive).
        anyOf:
          - type: integer
          - type: string
      min_length:
        type: integer
        description: Minimal length of the sequences, only counting valid characters.
      non_nucleotide:
        type: boolean
        description: Deprecated, please use 'exclude_invalid' instead.
      probabilistic_sampling:
        type: boolean
        description: Allow probabilistic sampling during subsampling.
      query:
        type: string
        description: Filter sequences by attribute. Uses Pandas DataFrame query syntax.
      query_columns:
        description: Use alongside query to specify columns and data types in the format 'column:type'.
        anyOf:
          - type: string
          - type: array
            items:
              type: string
      sequences_per_group:
        type: integer
        description: Select no more than this number of sequences per category.
type: object
description: RSV Phylogenetic Workflow Configuration Schema
additionalProperties: false
properties:
  additional_inputs:
    type: array
    description: Additional user-provided input datasets to merge with defaults
    items:
      $ref: '#/$defs/InputItem'
  ancestral:
    type: object
    description: Ancestral sequence reconstruction settings
    additionalProperties: false
    properties:
      inference:
        type: string
        description: Inference mode for ancestral sequences ('joint' or 'marginal')
  builds_to_run:
    type: array
    description: Target builds to run
    items:
      type: string
  cds:
    type: object
    description: Mapping of build names to their corresponding CDS gene name
    additionalProperties:
      type: string
      description: <build_name>
  conda_environment:
    type: string
    description: Path to conda environment definition file used by Snakemake
  custom_rules:
    type: array
    description: Custom Snakemake rule files to include (bypasses schema validation)
    items:
      type: string
  custom_subsample:
    type: object
    description: Custom subsampling configuration. When using --configfile, this is recommended over 'subsample' to ignore default subsampling configuration.
    additionalProperties:
      description: <build_name>
      $ref: '#/$defs/SubsampleConfigUnaligned'
  description:
    type: string
    description: Path to markdown description file displayed in Auspice
  display_strain_field:
    type: string
    description: Metadata column used for display names on tree tips
  dms_only_positive_escape:
    type: boolean
    description: Whether to clamp negative DMS escape scores to zero
  enrich_antibody_escape:
    type: object
    description: Criteria for enriching phylogenetic trees with high escape mutants
    additionalProperties:
      type: object
      description: <build_name>
      additionalProperties: false
      properties:
        group_by:
          type: array
          description: Metadata variables to group by when selecting escape sequences
          items:
            type: string
        max_identical_f_prot_muts:
          description: Maximum sequences per group with identical F protein mutations
          allOf:
            - type: number
            - type: integer
              minimum: 0
        max_identical_max_escape_mut:
          description: Maximum sequences per group with the same top escape mutation
          allOf:
            - type: number
            - type: integer
              minimum: 0
        nseqs_per_antibody_scoretype:
          description: Number of high-escape sequences to select per antibody score
          allOf:
            - type: number
            - type: integer
              exclusiveMinimum: 0
  exclude:
    type: string
    description: Path to file with outlier sequence accessions to exclude
  f_dms_antibodies:
    type: array
    description: Monoclonal antibodies to evaluate from DMS dataset
    items:
      type: string
  f_dms_data:
    type: string
    description: Path to deep mutational scanning (DMS) per-mutation escape CSV
  files:
    type: object
    description: Paths to Auspice configuration JSON files
    additionalProperties: false
    properties:
      auspice_config:
        type: string
        description: Main Auspice dataset configuration JSON
      auspice_config_additional_colorings:
        type: string
        description: Additional colorings configuration JSON
      auspice_config_f_antibody_escape:
        type: string
        description: Auspice configuration for F antibody escape DMS defaults
      auspice_config_non-genome_builds:
        type: string
        description: Auspice configuration for non-genome (gene-specific) builds
  filter_for_f_antibody_escape:
    type: object
    description: Sequence length and coverage filtering thresholds for antibody escape builds
    additionalProperties: false
    properties:
      group_by:
        type: string
        description: Metadata column name for grouping during escape sequence filtering
      min_coverage:
        type: object
        description: Minimum fraction coverage required per build
        additionalProperties:
          type: number
          description: <build_name>
          maximum: 1
          minimum: 0
      min_length:
        type: object
        description: Minimum sequence length per build
        additionalProperties:
          description: <build_name>
          allOf:
            - type: number
            - type: integer
              exclusiveMinimum: 0
      resolutions:
        type: object
        description: Date bounds per temporal resolution
        additionalProperties:
          type: object
          description: <resolution>
          additionalProperties: false
          properties:
            background_min_date:
              type: string
            min_date:
              type: string
  frequencies:
    type: object
    description: Clade/tip frequency estimation parameters per resolution
    additionalProperties: false
    properties:
      resolutions:
        type: object
        additionalProperties:
          type: object
          description: <resolution>
          additionalProperties: false
          properties:
            min_date:
              type: string
              description: Minimum/start date for frequency calculation window
  genesforglycosylation:
    type: array
    description: Genes to analyze for N-linked glycosylation site changes
    items:
      type: string
  inputs:
    type: array
    description: Default input datasets to fetch and merge
    items:
      $ref: '#/$defs/InputItem'
  nextclade_attributes:
    type: object
    description: Nextclade dataset reference attributes per subtype
    additionalProperties:
      type: object
      description: <subtype>
      additionalProperties: false
      properties:
        accession:
          type: string
          description: Reference sequence accession number
        name:
          type: string
          description: Human-readable Nextclade dataset display name
        reference_name:
          type: string
          description: Reference strain identifier
  refine:
    type: object
    description: TreeTime refine parameters for molecular clock and date inference
    additionalProperties: false
    properties:
      clock_filter_iqd:
        type: number
        description: Number of interquartile distances for clock outlier filtering
      coalescent:
        type: string
        description: Coalescent tree prior ('opt', 'skyline', or float)
      date_inference:
        type: string
        description: Date inference mode ('marginal' or 'joint')
      divergence_units:
        type: string
        description: Divergence units on phylogenetic tree ('mutations-per-site' or 'mutations')
  resolutions_to_run:
    type: array
    description: Temporal resolutions to build trees for
    items:
      type: string
  strain_id_field:
    type: string
    description: Metadata column identifying the unique sequence/strain ID
  subsample:
    type: object
    description: Subsampling configuration. When using --configfile, it is recommended to use 'custom_subsample' instead to ignore default subsampling configuration.
    additionalProperties:
      description: <build_name>
      $ref: '#/$defs/SubsampleConfigUnaligned'
  subtypes:
    type: array
    description: RSV subtypes to analyze (e.g. "a", "b")
    items:
      type: string
  traits:
    type: object
    description: Metadata traits for ancestral state reconstruction
    additionalProperties: false
    properties:
      columns:
        description: Column name(s) in metadata to reconstruct on the tree
        anyOf:
          - type: string
          - type: array
            items:
              type: string
